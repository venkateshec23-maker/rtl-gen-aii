module adder_8bit (
    input clk,
    input reset_n,
    input [7:0] a,
    input [7:0] b,
    output reg [8:0] sum
);

always @(posedge clk) begin
    if (!reset_n) begin
        sum <= 9'b0;
    end else begin
        sum <= a + b;
    end
end

endmodule
