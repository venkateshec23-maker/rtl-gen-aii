[pytest]
markers =
    unit: Fast unit tests - no Docker, no EDA tools (< 30 seconds)
    integration: Real integration tests - requires Docker and EDA tools (< 10 minutes)
    database: Database schema and metrics storage tests

testpaths = tests

# Default: run only unit tests
# For integration: pytest -m integration
addopts = -m unit --tb=short -v
